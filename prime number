for num in range(99, 9, -1):  # Checking numbers from 99 to 10
    is_prime = True
    for i in range(2, int(num**0.5) + 1):
        if num % i == 0:
            is_prime = False
            break
    if is_prime:
        print(f"The largest two-digit prime number is: {num}")
        break  
